# ─────────────────────────────────────────
# ⚖️ Mizan Framework SDK — Environment Setup
# ─────────────────────────────────────────
# Copy this file to .env and fill in your values
# cp .env.example .env

# ── LLM Providers (add at least one) ──────
OPENAI_API_KEY=              # Get from: https://platform.openai.com/api-keys
ANTHROPIC_API_KEY=           # Get from: https://console.anthropic.com

# ── Mizan Configuration ──────────────────
MIZAN_AUDIT_PATH=./data/audit.jsonl    # Where to store audit logs
MIZAN_MEMORY_PATH=./data/memory.json   # Where to store agent memory
MIZAN_DEFAULT_MODEL=gpt-4o-mini        # Default LLM model
MIZAN_LOG_LEVEL=info                   # debug | info | warn | error

# ── Optional: Search & RAG ───────────────
# MEILISEARCH_URL=http://localhost:7700
# MEILISEARCH_KEY=

# ── Optional: Notifications ──────────────
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ── Agent Builder Paths ──────────────────
MIZAN_PROJECT_DIR=.          # Root directory for agent files (default: SDK root)
MIZAN_AGENTS_PATH=./agents   # Where generated agent .js files are stored
